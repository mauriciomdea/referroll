<%= form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t('models.job.title') %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label t('models.job.desc') %><br>
    <%= f.text_field :desc %>
  </div>
  <div class="field">
    <%= f.label t('models.job.company') %><br>
    <%= f.text_field :company %>
  </div>
  <div class="field">
    <%= f.label t('models.job.status') %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit t('helpers.submit.create', model: t('models.job.one')), class: 'btn btn-primary' %>
  </div>
<% end %>
